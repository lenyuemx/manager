<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.QuitApplyMapper">

    <sql id="Base_Column_List">
        id, user_id, department_id, reason, apply_time, status
    </sql>

    <select id="selectAll" resultType="com.example.entity.QuitApply">
        select
        qa.*, u.name as userName, d.name as departmentName
        from quit_apply qa
        left join user u on qa.user_id = u.id
        left join department d on qa.department_id = d.id
        <where>
            <if test="id != null"> and qa.id = #{id}</if>
            <if test="userId != null"> and qa.user_id = #{userId}</if>
            <if test="departmentId != null"> and qa.department_id = #{departmentId}</if>
            <if test="status != null and status != ''"> and qa.status = #{status}</if>
            <if test="userName != null and userName != ''"> and u.name like concat('%', #{userName}, '%')</if>
        </where>
        order by qa.id desc
    </select>

    <select id="selectById" resultType="com.example.entity.QuitApply">
        select
        <include refid="Base_Column_List" />
        from quit_apply
        where id = #{id}
    </select>

    <delete id="deleteById">
        delete from quit_apply
        where id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.QuitApply" useGeneratedKeys="true" keyProperty="id">
        insert into quit_apply
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="departmentId != null">department_id,</if>
            <if test="reason != null">reason,</if>
            <if test="applyTime != null">apply_time,</if>
            <if test="status != null">status,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="departmentId != null">#{departmentId},</if>
            <if test="reason != null">#{reason},</if>
            <if test="applyTime != null">#{applyTime},</if>
            <if test="status != null">#{status},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.QuitApply">
        update quit_apply
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="departmentId != null">department_id = #{departmentId},</if>
            <if test="reason != null">reason = #{reason},</if>
            <if test="applyTime != null">apply_time = #{applyTime},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        where id = #{id}
    </update>

</mapper>
