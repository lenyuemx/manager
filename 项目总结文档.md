# 基于SpringBoot的大学社团管理系统 — 项目总结文档

## 一、项目概述

本项目是一个**大学社团管理系统**，采用前后端分离架构，后端基于 **Spring Boot 2.5.9 + MyBatis**，前端基于 **Vue 2 + Element UI**。系统支持**管理员（ADMIN）**和**学生用户（USER）**两种角色，学生角色下又区分**社长（HEADER）**和**普通学生（STUDENT）**两种权限等级。

---

## 二、技术栈

### 2.1 后端

| 技术 | 版本 | 说明 |
|---|---|---|
| Spring Boot | 2.5.9 | 基础框架 |
| MyBatis | 2.2.1 | ORM 持久层框架 |
| PageHelper | 1.4.6 | MyBatis 分页插件 |
| MySQL | - | 关系型数据库 |
| Hutool | 5.8.18 | Java 工具类库 |
| java-jwt (auth0) | 4.3.0 | JWT 令牌认证 |
| Java | 1.8 | JDK 版本 |

### 2.2 前端

| 技术 | 版本 | 说明 |
|---|---|---|
| Vue | 2.6.14 | 前端框架 |
| Vue Router | 3.5.1 | 路由管理 |
| Element UI | 2.15.14 | UI 组件库 |
| Axios | 1.5.1 | HTTP 请求库 |
| WangEditor | 4.7.15 | 富文本编辑器 |
| Vue CLI | 5.0 | 构建工具 |

### 2.3 数据库

- **MySQL**，数据库名：`association_manager`
- 字符集：`utf8mb4`

---

## 三、项目目录结构

```
基于SpringBoot的大学社团管理系统的设计与实现/
├── admin.jpg                          # 管理员界面截图
├── manager.sql                        # 数据库初始化脚本
└── manager/                           # 主项目目录
    ├── springboot/                     # 后端 Spring Boot 项目
    │   ├── pom.xml                    # Maven 依赖管理
    │   └── src/main/
    │       ├── java/com/example/
    │       │   ├── SpringbootApplication.java   # 启动类
    │       │   ├── common/                      # 通用模块
    │       │   │   ├── Constants.java           # 常量定义
    │       │   │   ├── Result.java              # 统一返回结果
    │       │   │   ├── config/                  # 配置类
    │       │   │   │   ├── CorsConfig.java      # 跨域配置
    │       │   │   │   ├── JwtInterceptor.java  # JWT 拦截器
    │       │   │   │   └── WebConfig.java       # Web 配置
    │       │   │   └── enums/                   # 枚举类
    │       │   │       ├── ApplyEnum.java        # 申请状态枚举
    │       │   │       ├── LevelEnum.java        # 用户等级枚举
    │       │   │       ├── ResultCodeEnum.java   # 结果码枚举
    │       │   │       └── RoleEnum.java         # 角色枚举
    │       │   ├── controller/                   # 控制器层（11个）
    │       │   │   ├── WebController.java        # 登录/注册/改密
    │       │   │   ├── AdminController.java      # 管理员管理
    │       │   │   ├── UserController.java       # 用户管理
    │       │   │   ├── DepartmentController.java # 社团管理
    │       │   │   ├── ActivityController.java   # 活动管理
    │       │   │   ├── InformationController.java# 资讯管理
    │       │   │   ├── ApplyController.java      # 申请审批
    │       │   │   ├── NoticeController.java     # 公告管理
    │       │   │   ├── CarouselController.java   # 轮播图管理
    │       │   │   ├── CommentController.java    # 评论管理
    │       │   │   └── FileController.java       # 文件上传/下载
    │       │   ├── entity/                       # 实体类（11个）
    │       │   │   ├── Account.java              # 账户基类
    │       │   │   ├── Admin.java                # 管理员
    │       │   │   ├── User.java                 # 学生用户
    │       │   │   ├── Department.java           # 社团
    │       │   │   ├── Activity.java             # 活动
    │       │   │   ├── Information.java          # 资讯
    │       │   │   ├── Apply.java                # 申请
    │       │   │   ├── Notice.java               # 公告
    │       │   │   ├── Carousel.java             # 轮播图
    │       │   │   ├── Comment.java              # 评论
    │       │   │   └── UserLike.java             # 用户点赞
    │       │   ├── mapper/                       # 数据访问层（10个）
    │       │   ├── service/                      # 业务逻辑层（10个）
    │       │   ├── exception/                    # 异常处理
    │       │   │   ├── BusinessException.java
    │       │   │   ├── CustomException.java
    │       │   │   └── GlobalExceptionHandler.java
    │       │   └── utils/
    │       │       └── TokenUtils.java           # Token 工具类
    │       └── resources/
    │           ├── application.yml                # 应用配置
    │           └── mapper/                        # MyBatis XML映射（10个）
    └── vue/                            # 前端 Vue 项目
        ├── package.json
        ├── vue.config.js
        └── src/
            ├── App.vue
            ├── main.js
            ├── router/index.js          # 路由配置
            ├── utils/                   # 工具类（Axios封装）
            ├── assets/                  # 静态资源（图片/CSS）
            └── views/                   # 页面组件
                ├── Login.vue            # 登录页
                ├── Register.vue         # 注册页
                ├── 404.vue              # 404 页
                ├── Front.vue            # 前台布局
                ├── Manager.vue          # 后台管理布局
                ├── front/               # 前台页面（7个）
                │   ├── Home.vue         # 前台首页
                │   ├── Person.vue       # 个人中心
                │   ├── Apply.vue        # 我的申请
                │   ├── DepartmentDetail.vue  # 社团详情
                │   ├── ActivityDetail.vue    # 活动详情
                │   ├── InformationDetail.vue # 资讯详情
                │   └── Comment.vue      # 评论组件
                └── manager/             # 后台管理页面（14个）
                    ├── Home.vue         # 后台首页
                    ├── Admin.vue        # 管理员管理
                    ├── User.vue         # 用户管理
                    ├── Department.vue   # 社团管理
                    ├── Activity.vue     # 活动管理
                    ├── Information.vue  # 资讯管理
                    ├── Apply.vue        # 申请审批
                    ├── Member.vue       # 社团成员
                    ├── Notice.vue       # 公告管理
                    ├── Carousel.vue     # 轮播图管理
                    ├── AdminPerson.vue  # 管理员个人信息
                    ├── UserPerson.vue   # 用户个人信息
                    ├── Password.vue     # 修改密码
                    └── 403.vue          # 无权限页
```

---

## 四、数据库设计

### 4.1 数据表清单

| 表名 | 说明 | 主要字段 |
|---|---|---|
| `admin` | 管理员表 | id, username, password, name, avatar, role, phone, email |
| `notice` | 公告信息表 | id, title, content, time, user |
| `carousel` | 轮播图表 | id, img |
| `department` | 社团信息表（推断） | id, img, name, description, user_id, time |
| `activity` | 社团活动表（推断） | id, img, name, description, department_id, time, views, likes |
| `information` | 社团资讯表（推断） | id, name, description, department_id, time, views, likes |
| `apply` | 申请审批表（推断） | id, user_id, department_id, description, process, status, note |
| `user` | 学生用户表（推断） | id, username, password, name, phone, email, avatar, role, level, department_id |
| `comment` | 评论表 | id, content, user_id, time, parent_id, likes, foreign_id, module |
| `user_like` | 用户点赞表 | id, user_id, foreign_id, module（联合唯一索引） |

### 4.2 ER关系概述

- **Department ↔ User**：每个社团有一个社长（user_id），社长与社团一一对应
- **Activity → Department**：活动归属于某个社团
- **Information → Department**：资讯归属于某个社团
- **Apply → User + Department**：用户申请加入社团
- **Comment → User**：评论关联评论人，支持树形父子结构（parent_id）
- **UserLike → User**：用户点赞记录，通过 module 字段区分点赞对象类型

---

## 五、系统架构与分层

```
┌─────────────────────────────────────────────┐
│                 Vue 前端                      │
│  (Element UI + Vue Router + Axios)           │
└──────────────────┬──────────────────────────┘
                   │ HTTP REST API (JSON)
                   │ Port: 8080 → 代理到 9090
┌──────────────────▼──────────────────────────┐
│              Spring Boot 后端                 │
│  ┌───────────┐ ┌──────────┐ ┌────────────┐  │
│  │ Controller│→│ Service  │→│   Mapper   │  │
│  │  (REST)   │ │ (业务)   │ │ (MyBatis)  │  │
│  └───────────┘ └──────────┘ └─────┬──────┘  │
│  ┌─────────────────────────────┐  │         │
│  │ JWT拦截器 / 跨域配置 / 异常处理│  │         │
│  └─────────────────────────────┘  │         │
└───────────────────────────────────┼─────────┘
                                    │
                   ┌────────────────▼──────────┐
                   │       MySQL 数据库          │
                   │   association_manager      │
                   └───────────────────────────┘
```

---

## 六、核心功能模块

### 6.1 用户认证与权限

- **登录**：支持管理员（ADMIN）和学生（USER）两种角色登录（`/login`）
- **注册**：仅学生用户可注册（`/register`）
- **JWT 认证**：登录成功后生成 JWT Token（2小时有效），以用户密码作为签名密钥
- **拦截器**：`JwtInterceptor` 对需要认证的接口进行 Token 校验
- **角色控制**：前端通过 `user.role` 控制菜单可见性；后端通过 `TokenUtils.getCurrentUser()` 获取当前用户并做权限过滤

### 6.2 社团管理

- 社团信息的 CRUD（增删改查 + 分页）
- 社长与社团**一一对应**校验
- 社长角色只能管理自己的社团

### 6.3 申请审批流程

- 学生提交入社申请 → 状态设为"社长审核中" + "待审核"
- **防重复**：同一学生不能重复申请同一社团
- 社长审核：通过 / 不通过，支持备注
- 审核状态枚举：`待审核`、`审核通过`、`审核不通过`

### 6.4 社团活动管理

- 活动信息的 CRUD + 分页查询
- 活动关联所属社团
- 支持**浏览量**和**点赞数**统计

### 6.5 社团资讯管理

- 资讯信息的 CRUD + 分页查询
- 资讯关联所属社团
- 支持**浏览量**和**点赞数**统计

### 6.6 评论系统

- 支持对活动/资讯等模块发表评论
- **树形评论**：通过 `parent_id` 实现评论的父子层级关系
- 评论支持点赞
- 通过 `module` 字段区分评论所属模块（活动/资讯等）

### 6.7 点赞系统

- `user_like` 表记录用户点赞
- 联合唯一索引 (`user_id`, `foreign_id`, `module`) 防止重复点赞
- 通过 `module` 字段区分点赞对象类型

### 6.8 公告管理

- 公告信息的 CRUD（仅管理员可操作）
- 前台展示公告轮播

### 6.9 轮播图管理

- 轮播图的上传和管理（仅管理员可操作）

### 6.10 文件管理

- 文件上传：存储至服务器 `files/` 目录，文件名以 `时间戳-原文件名` 格式存储
- 文件下载/获取：通过 URL 直接访问
- 集成 WangEditor 富文本编辑器上传接口

### 6.11 用户管理

- 管理员管理（仅 ADMIN 角色可见）
- 学生用户管理（仅 ADMIN 角色可见）
- 个人信息编辑、修改密码

---

## 七、前端路由设计

### 7.1 后台管理路由（`/` 根路径）

| 路径 | 页面 | 权限 |
|---|---|---|
| `/home` | 系统首页 | 所有登录用户 |
| `/admin` | 管理员信息 | ADMIN |
| `/user` | 用户信息 | ADMIN |
| `/notice` | 公告信息 | ADMIN |
| `/carousel` | 轮播图管理 | ADMIN |
| `/department` | 社团信息 | ADMIN + 社长 |
| `/apply` | 申请审批 | ADMIN + 社长 |
| `/member` | 社团成员 | ADMIN + 社长 |
| `/activity` | 社团活动 | ADMIN + 社长 |
| `/information` | 社团资讯 | ADMIN + 社长 |
| `/adminPerson` | 管理员个人信息 | ADMIN |
| `/userPerson` | 用户个人信息 | USER |
| `/password` | 修改密码 | 所有登录用户 |

### 7.2 前台路由（`/front`）

| 路径 | 页面 |
|---|---|
| `/front/home` | 前台首页（社团展示 + 活动 + 资讯） |
| `/front/person` | 个人中心 |
| `/front/apply` | 我的申请 |
| `/front/departmentDetail` | 社团详情 |
| `/front/activityDetail` | 活动详情 |
| `/front/informationDetail` | 资讯详情 |

---

## 八、API 接口汇总

### 8.1 公共接口

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/login` | 登录 |
| POST | `/register` | 注册 |
| PUT | `/updatePassword` | 修改密码 |

### 8.2 业务模块接口（统一 RESTful 风格）

每个业务模块均提供以下标准接口：

| 方法 | 路径模式 | 说明 |
|---|---|---|
| POST | `/{module}/add` | 新增 |
| DELETE | `/{module}/delete/{id}` | 单条删除 |
| DELETE | `/{module}/delete/batch` | 批量删除 |
| PUT | `/{module}/update` | 修改 |
| GET | `/{module}/selectById/{id}` | 按 ID 查询 |
| GET | `/{module}/selectAll` | 查询全部 |
| GET | `/{module}/selectPage` | 分页查询 |

**涉及模块**：`admin`, `user`, `department`, `activity`, `information`, `apply`, `notice`, `carousel`, `comment`

### 8.3 特殊接口

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/files/upload` | 文件上传 |
| GET | `/files/{flag}` | 文件下载/获取 |
| DELETE | `/files/{flag}` | 文件删除 |
| POST | `/files/wang/upload` | WangEditor 图片上传 |
| GET | `/comment/select/tree` | 查询树形评论 |
| GET | `/apply/selectMyApply` | 查询当前用户的申请 |

---

## 九、运行配置

### 9.1 后端配置（application.yml）

- **端口**：`9090`
- **数据库**：`localhost:3306/association_manager`，用户名 `root`，密码 `123456`
- **文件上传大小限制**：100MB
- **MyBatis**：开启驼峰映射、SQL 日志输出

### 9.2 前端配置

- **开发环境代理**：前端端口 8080，API 请求代理至 `http://localhost:9090`
- **路由模式**：`history` 模式
- **本地存储**：用户信息存储在 `localStorage`，key 为 `xm-user`

---

## 十、代码统计

| 类别 | 文件数 |
|---|---|
| Java 实体类（Entity） | 11 |
| Mapper 接口 | 10 |
| MyBatis XML 映射 | 10 |
| Service 业务类 | 10 |
| Controller 控制器 | 11 |
| 配置/工具/异常类 | 7 |
| Vue 页面组件 | 26 |
| **总计** | **约 85 个源文件** |

---

## 十一、系统角色与权限矩阵

| 功能 | 管理员 (ADMIN) | 社长 (USER-HEADER) | 普通学生 (USER-STUDENT) |
|---|:---:|:---:|:---:|
| 公告管理 | ✅ | ❌ | ❌ |
| 轮播图管理 | ✅ | ❌ | ❌ |
| 管理员信息管理 | ✅ | ❌ | ❌ |
| 用户信息管理 | ✅ | ❌ | ❌ |
| 社团信息管理 | ✅ | ✅（仅自己的社团） | ❌ |
| 申请审批 | ✅ | ✅（仅自己社团的申请） | ❌ |
| 社团成员管理 | ✅ | ✅ | ❌ |
| 社团活动管理 | ✅ | ✅ | ❌ |
| 社团资讯管理 | ✅ | ✅ | ❌ |
| 前台浏览 | ✅ | ✅ | ✅ |
| 申请入社 | ❌ | ✅ | ✅ |
| 评论/点赞 | ✅ | ✅ | ✅ |
| 个人信息修改 | ✅ | ✅ | ✅ |

---

## 十二、关键设计特点

1. **前后端分离**：后端提供 RESTful API，前端通过 Axios 调用，跨域通过 `CorsConfig` 解决
2. **JWT 无状态认证**：Token 中存储 `userId-role`，密码作为签名密钥，2小时过期
3. **统一返回格式**：`Result` 类封装所有 API 响应（code + msg + data）
4. **全局异常处理**：`GlobalExceptionHandler` 统一捕获并返回友好错误信息
5. **分页封装**：基于 PageHelper 实现，前端传入 `pageNum` 和 `pageSize`
6. **权限数据过滤**：Service 层根据当前登录用户角色自动过滤数据范围
7. **文件管理**：基于本地文件系统，以时间戳防重名
8. **树形评论**：支持多级评论嵌套展示

---

*文档生成时间：2026年3月27日*
